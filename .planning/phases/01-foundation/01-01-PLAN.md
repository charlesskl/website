---
phase: 01-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/animation/controller.js
  - assets/animation/scroll-engine.js
  - assets/animation/responsive.js
  - index.html
  - about.html
  - contact.html
  - careers.html
  - capabilities/costume.html
  - capabilities/dolls.html
  - capabilities/plastic-toys.html
  - capabilities/plush.html
  - capabilities/rc-vehicle.html
  - cn/index.html
  - cn/about.html
  - cn/contact.html
  - cn/careers.html
  - cn/capabilities/costume.html
  - cn/capabilities/dolls.html
  - cn/capabilities/plastic-toys.html
  - cn/capabilities/plush.html
  - cn/capabilities/rc-vehicle.html
  - id/index.html
  - id/about.html
  - id/contact.html
  - id/careers.html
  - id/capabilities/costume.html
  - id/capabilities/dolls.html
  - id/capabilities/plastic-toys.html
  - id/capabilities/plush.html
  - id/capabilities/rc-vehicle.html
autonomous: false
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-04
  - FOUND-05
  - FOUND-06
  - FOUND-07
  - FOUND-08

must_haves:
  truths:
    - "window.RR exists with register/initAll/killAll/refreshAll and modules array"
    - "Lenis smooth scroll runs without drift — ScrollTrigger positions match visual positions"
    - "prefers-reduced-motion disables all animations — all content still visible and accessible"
    - "Removing any CDN script tag does not break page rendering"
    - "Every HTML body tag has a data-page attribute (home/about/contact/careers/capabilities-*)"
    - "Above-the-fold elements have gsap.set() initial states applied before paint"
    - "Text animations only run after document.fonts.ready resolves"
    - "All ScrollTrigger instances are created inside gsap.matchMedia() contexts"
  artifacts:
    - path: "assets/animation/controller.js"
      provides: "window.RR namespace and module registry with init/kill/refresh lifecycle"
      exports: ["window.RR"]
      contains: "register, initAll, killAll, refreshAll, data-page detection"
    - path: "assets/animation/scroll-engine.js"
      provides: "Lenis 1.3.18 + GSAP ScrollTrigger single RAF loop"
      contains: "lenis.on('scroll', ScrollTrigger.update), gsap.ticker.add, gsap.ticker.lagSmoothing(0)"
    - path: "assets/animation/responsive.js"
      provides: "Centralized gsap.matchMedia() contexts with prefers-reduced-motion gate"
      contains: "gsap.matchMedia(), isDesktop, isMobile, reducedMotion conditions"
  key_links:
    - from: "assets/animation/scroll-engine.js"
      to: "ScrollTrigger"
      via: "lenis.on('scroll', ScrollTrigger.update)"
      pattern: "lenis\\.on.*ScrollTrigger\\.update"
    - from: "assets/animation/scroll-engine.js"
      to: "Lenis RAF"
      via: "gsap.ticker.add(time => lenis.raf(time * 1000))"
      pattern: "gsap\\.ticker\\.add"
    - from: "assets/animation/controller.js"
      to: "page detection"
      via: "document.body.dataset.page"
      pattern: "dataset\\.page"
    - from: "HTML body tags"
      to: "controller.js page routing"
      via: "data-page attribute"
      pattern: "data-page="
---

<objective>
Build the animation infrastructure foundation: three new JS modules (controller, scroll-engine, responsive), wire them into all 27 HTML pages (en/cn/id variants), add data-page attributes to all body tags, and upgrade Lenis from 1.1.18 to 1.3.18.

Purpose: Every subsequent animation phase depends on this infrastructure being correct first. A broken Lenis/ScrollTrigger sync causes 100-200px trigger drift that cannot be patched at higher layers.

Output: Three new files in assets/animation/, all 27 HTML pages updated with data-page body attributes and new script tags, Lenis upgraded to 1.3.18 on the 9 files that currently have 1.1.18.
</objective>

<execution_context>
@/Users/charles/.claude/get-shit-done/workflows/execute-plan.md
@/Users/charles/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<!-- Key architectural patterns confirmed from research: -->
<!-- 1. Lenis/GSAP 3-line sync boilerplate: -->
<!--    lenis.on('scroll', ScrollTrigger.update) -->
<!--    gsap.ticker.add((time) => lenis.raf(time * 1000)) -->
<!--    gsap.ticker.lagSmoothing(0) -->
<!-- 2. window.RR global registry pattern (no bundler) -->
<!-- 3. data-page on body for page-specific targeting -->
<!-- 4. gsap.matchMedia() wrapping ALL ScrollTriggers -->
<!-- 5. document.fonts.ready wrapping text animation inits -->

<interfaces>
<!-- Existing shared.js exposes these (executor must not duplicate): -->
<!-- - initCursor(), initMagneticButtons(), initScrollReveal() -->
<!-- - window.matchMedia('(prefers-reduced-motion: reduce)').matches pattern already used -->
<!-- - GSAP loaded at GSAP 3.14.2 via CDN in root pages, 3.12.7 in cn/id pages (upgrade task in Task 2) -->

<!-- CDN loading order established: -->
<!-- Lenis → Splitting.js → shared.js → GSAP → ScrollTrigger → animations.js -->
<!-- New files slot AFTER animations.js: controller.js → scroll-engine.js → responsive.js -->

<!-- 27 HTML files needing data-page attribute (no data-page exists on ANY body tag currently): -->
<!-- Root (en): index.html, about.html, contact.html, careers.html -->
<!-- Root capabilities: capabilities/costume.html, dolls.html, plastic-toys.html, plush.html, rc-vehicle.html -->
<!-- CN: cn/index.html, cn/about.html, cn/contact.html, cn/careers.html + 5 cn/capabilities/ -->
<!-- ID: id/index.html, id/about.html, id/contact.html, id/careers.html + 5 id/capabilities/ -->
<!-- admin.html is EXCLUDED (not part of animation system) -->

<!-- Lenis CDN situation: -->
<!-- 9 files currently have lenis@1.1.18 (all root + root capabilities pages) -->
<!-- cn/index.html and id/index.html have a <!-- Lenis Smooth Scroll --> comment but NO script tag -->
<!-- cn/about.html, cn/contact.html, cn/careers.html, cn/capabilities/*, id/about.html etc. have NO Lenis tag -->
<!-- All cn/ and id/ non-index pages must get Lenis 1.3.18 added for the first time -->

<!-- Relative path pattern for assets from subdirectories: -->
<!-- /cn/ pages use: ../assets/shared.js, ../assets/animation/controller.js -->
<!-- /cn/capabilities/ pages use: ../../assets/shared.js, ../../assets/animation/controller.js -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create animation module files (controller.js, scroll-engine.js, responsive.js)</name>
  <files>
    assets/animation/controller.js
    assets/animation/scroll-engine.js
    assets/animation/responsive.js
  </files>
  <action>
Create the directory `assets/animation/` and three files. All files use IIFE pattern with `'use strict'`. All CDN dependencies wrapped in try/catch for graceful degradation (FOUND-05).

**assets/animation/controller.js** — window.RR module registry (FOUND-01):
```javascript
(function () {
  'use strict';

  // Establish global namespace — idempotent if called multiple times
  window.RR = window.RR || {};

  // Module registry with init/kill/refresh lifecycle
  window.RR.modules = window.RR.modules || [];

  window.RR.register = function (name, mod) {
    // Prevent duplicate registration
    var exists = window.RR.modules.some(function (m) { return m.name === name; });
    if (!exists) {
      window.RR.modules.push({ name: name, init: mod.init, kill: mod.kill, refresh: mod.refresh });
    }
  };

  window.RR.initAll = function () {
    window.RR.modules.forEach(function (m) {
      try {
        if (typeof m.init === 'function') m.init();
      } catch (e) {
        console.warn('[RR] Module init failed:', m.name, e);
      }
    });
  };

  window.RR.killAll = function () {
    window.RR.modules.forEach(function (m) {
      try {
        if (typeof m.kill === 'function') m.kill();
      } catch (e) {
        console.warn('[RR] Module kill failed:', m.name, e);
      }
    });
  };

  window.RR.refreshAll = function () {
    window.RR.modules.forEach(function (m) {
      try {
        if (typeof m.refresh === 'function') m.refresh();
      } catch (e) {
        console.warn('[RR] Module refresh failed:', m.name, e);
      }
    });
  };

  // Page detection — read data-page from body for conditional module init
  window.RR.page = document.body.dataset.page || '';

  // Shared state object for cross-module communication
  window.RR.state = window.RR.state || {
    isTransitioning: false,
    scrollY: 0,
    hasReducedMotion: window.matchMedia('(prefers-reduced-motion: reduce)').matches
  };

  // FOUC prevention: gsap.set() initial states for above-the-fold elements (FOUND-08)
  // Run before DOMContentLoaded so states are applied before paint
  // Only run if GSAP is available (CDN graceful degradation)
  function applyInitialStates() {
    if (typeof gsap === 'undefined') return; // CDN failed — no FOUC prevention needed (elements already visible)

    if (window.RR.state.hasReducedMotion) return; // Skip — show content immediately

    // Hero content elements (hide before animation takes over in Phase 2)
    // Set opacity:0 on elements that Phase 2 hero.js will animate in
    // Using data attributes so Phase 2 can target without guessing selectors
    var heroTitle = document.querySelector('.hero-title');
    var heroSub   = document.querySelector('.hero-sub');
    var heroActions = document.querySelector('.hero-actions');
    var heroTag   = document.querySelector('.hero-tag');

    if (heroTitle)   gsap.set(heroTitle,   { opacity: 0, y: 30 });
    if (heroSub)     gsap.set(heroSub,     { opacity: 0, y: 20 });
    if (heroActions) gsap.set(heroActions, { opacity: 0, y: 15 });
    if (heroTag)     gsap.set(heroTag,     { opacity: 0, y: 10 });
  }

  // document.fonts.ready wrapper — gates text animation readiness (FOUND-06)
  // Attach to RR so text-animations.js (Phase 2) can use it
  window.RR.onFontsReady = function (callback) {
    if (document.fonts && document.fonts.ready) {
      document.fonts.ready.then(callback).catch(function () {
        // fonts.ready rejected (rare) — proceed without font guarantee
        callback();
      });
    } else {
      // Older browsers without fonts.ready — fire immediately
      callback();
    }
  };

  document.addEventListener('DOMContentLoaded', function () {
    try {
      applyInitialStates();
    } catch (e) {
      // Never block page render for animation failures
      console.warn('[RR] Initial state failed:', e);
    }
  });

}());
```

**assets/animation/scroll-engine.js** — Lenis + GSAP ticker bridge (FOUND-02):
```javascript
(function () {
  'use strict';

  function initScrollEngine() {
    // Graceful degradation: if Lenis or GSAP is missing, fall back to native scroll (FOUND-05)
    if (typeof Lenis === 'undefined') {
      console.warn('[RR:scroll-engine] Lenis not loaded — using native scroll');
      return;
    }
    if (typeof gsap === 'undefined' || typeof ScrollTrigger === 'undefined') {
      console.warn('[RR:scroll-engine] GSAP/ScrollTrigger not loaded — Lenis running standalone');
      // Initialize Lenis without GSAP sync (still get smooth scroll)
      var lenisFallback = new Lenis({ lerp: 0.08, smoothWheel: true });
      window.RR.lenis = lenisFallback;
      function rafFallback(time) { lenisFallback.raf(time); requestAnimationFrame(rafFallback); }
      requestAnimationFrame(rafFallback);
      return;
    }

    try {
      // Register ScrollTrigger plugin
      gsap.registerPlugin(ScrollTrigger);

      // Initialize Lenis with project settings
      var lenis = new Lenis({
        lerp: 0.08,           // Smoothing factor (0 = instant, 1 = never arrives)
        smoothWheel: true,    // Smooth mouse wheel
        syncTouch: false      // Native touch on mobile — no interference
      });

      // THE 3-LINE SYNC (do not reorder):
      // 1. Tell ScrollTrigger to update positions when Lenis fires its scroll event
      lenis.on('scroll', ScrollTrigger.update);

      // 2. Plug Lenis RAF into GSAP ticker — single shared RAF loop, no drift
      gsap.ticker.add(function (time) {
        lenis.raf(time * 1000); // GSAP time is in seconds; Lenis expects ms
      });

      // 3. Disable GSAP lag smoothing — prevents jitter during fast scroll
      gsap.ticker.lagSmoothing(0);

      // Expose lenis instance for cross-module access (webgl.js, cursor.js etc.)
      window.RR.lenis = lenis;

      // Keep scroll state in sync for velocity-based effects (Phase 3)
      lenis.on('scroll', function (e) {
        if (window.RR.state) window.RR.state.scrollY = e.scroll;
      });

      console.log('[RR:scroll-engine] Lenis + ScrollTrigger sync initialized');
    } catch (e) {
      console.warn('[RR:scroll-engine] Scroll engine init failed:', e);
      // Do NOT re-throw — page must still render and function
    }
  }

  document.addEventListener('DOMContentLoaded', initScrollEngine);

}());
```

**assets/animation/responsive.js** — centralized gsap.matchMedia() (FOUND-03, FOUND-04):
```javascript
(function () {
  'use strict';

  function initResponsive() {
    // Graceful degradation: gsap.matchMedia requires GSAP 3.11+
    if (typeof gsap === 'undefined' || typeof gsap.matchMedia !== 'function') {
      console.warn('[RR:responsive] gsap.matchMedia not available — responsive contexts skipped');
      return;
    }

    try {
      var mm = gsap.matchMedia();

      // Store mm context on RR for modules to add their own conditions
      window.RR.mm = mm;

      // Central breakpoint + motion preference contexts
      // ALL ScrollTrigger instances in Phase 2+ must be created inside one of these contexts
      mm.add({
        isDesktop:      '(min-width: 768px)',
        isMobile:       '(max-width: 767px)',
        isCoarsePointer:'(pointer: coarse)',           // touch devices — disable complex effects
        reducedMotion:  '(prefers-reduced-motion: reduce)' // accessibility gate (FOUND-04)
      }, function (context) {
        var conditions = context.conditions;

        // Update shared state so other modules can read motion preference
        if (window.RR.state) {
          window.RR.state.hasReducedMotion = conditions.reducedMotion;
        }

        // REDUCED MOTION GATE (FOUND-04):
        // If user prefers reduced motion, immediately reveal all animated elements
        // and skip all animation setup. Content remains fully accessible.
        if (conditions.reducedMotion) {
          document.querySelectorAll('[data-reveal]').forEach(function (el) {
            el.classList.add('is-revealed');
          });
          // Reset any gsap.set() initial states applied by controller.js
          // so above-fold content is immediately visible
          var immediateTargets = document.querySelectorAll('.hero-title, .hero-sub, .hero-actions, .hero-tag');
          if (typeof gsap !== 'undefined' && immediateTargets.length) {
            gsap.set(immediateTargets, { clearProps: 'all' });
          }
          return; // Skip all animation registration
        }

        // Expose conditions for other modules via window.RR.breakpoints
        window.RR.breakpoints = {
          isDesktop:       conditions.isDesktop,
          isMobile:        conditions.isMobile,
          isCoarsePointer: conditions.isCoarsePointer,
          reducedMotion:   conditions.reducedMotion
        };

        // GSAP matchMedia auto-reverts all animations created inside this callback
        // on breakpoint change — no manual cleanup needed in modules.
        // Phase 2+ modules should call window.RR.mm.add() to register their own contexts.

        // Cleanup function — returned from matchMedia callback
        // Called automatically by GSAP when context reverts
        return function () {
          window.RR.breakpoints = null;
        };
      });

      console.log('[RR:responsive] gsap.matchMedia contexts registered');
    } catch (e) {
      console.warn('[RR:responsive] Responsive init failed:', e);
    }
  }

  document.addEventListener('DOMContentLoaded', initResponsive);

}());
```
  </action>
  <verify>
    <automated>node -e "
const fs = require('fs');
const files = [
  'assets/animation/controller.js',
  'assets/animation/scroll-engine.js',
  'assets/animation/responsive.js'
];
let pass = true;
files.forEach(f => {
  if (!fs.existsSync('/Users/charles/Projects/royal-regent/rr-website/' + f)) {
    console.error('MISSING: ' + f); pass = false;
  } else {
    const src = fs.readFileSync('/Users/charles/Projects/royal-regent/rr-website/' + f, 'utf8');
    console.log('OK (' + src.length + ' bytes): ' + f);
  }
});

// Check key patterns
const ctrl = fs.readFileSync('/Users/charles/Projects/royal-regent/rr-website/assets/animation/controller.js', 'utf8');
const scroll = fs.readFileSync('/Users/charles/Projects/royal-regent/rr-website/assets/animation/scroll-engine.js', 'utf8');
const resp = fs.readFileSync('/Users/charles/Projects/royal-regent/rr-website/assets/animation/responsive.js', 'utf8');

if (!ctrl.includes('window.RR')) { console.error('FAIL: window.RR missing in controller.js'); pass=false; }
if (!ctrl.includes('register')) { console.error('FAIL: register() missing in controller.js'); pass=false; }
if (!ctrl.includes('initAll')) { console.error('FAIL: initAll() missing in controller.js'); pass=false; }
if (!ctrl.includes('killAll')) { console.error('FAIL: killAll() missing in controller.js'); pass=false; }
if (!ctrl.includes('onFontsReady')) { console.error('FAIL: onFontsReady missing in controller.js'); pass=false; }
if (!ctrl.includes('gsap.set')) { console.error('FAIL: gsap.set() FOUC prevention missing in controller.js'); pass=false; }
if (!scroll.includes('lenis.raf(time * 1000)')) { console.error('FAIL: GSAP ticker bridge missing in scroll-engine.js'); pass=false; }
if (!scroll.includes('ScrollTrigger.update')) { console.error('FAIL: lenis.on scroll sync missing in scroll-engine.js'); pass=false; }
if (!scroll.includes('lagSmoothing(0)')) { console.error('FAIL: lagSmoothing(0) missing in scroll-engine.js'); pass=false; }
if (!resp.includes('gsap.matchMedia')) { console.error('FAIL: gsap.matchMedia missing in responsive.js'); pass=false; }
if (!resp.includes('reducedMotion')) { console.error('FAIL: reducedMotion gate missing in responsive.js'); pass=false; }
if (!resp.includes('prefers-reduced-motion')) { console.error('FAIL: prefers-reduced-motion query missing in responsive.js'); pass=false; }

console.log(pass ? 'ALL CHECKS PASSED' : 'CHECKS FAILED');
process.exit(pass ? 0 : 1);
"
    </automated>
  </verify>
  <done>Three files exist in assets/animation/. Controller has window.RR registry with register/initAll/killAll, document.fonts.ready wrapper, and gsap.set() FOUC states. Scroll engine has the 3-line Lenis/GSAP sync boilerplate. Responsive has gsap.matchMedia() with reducedMotion gate that immediately reveals all content on prefers-reduced-motion.</done>
</task>

<task type="auto">
  <name>Task 2: Wire modules into all 27 HTML files (data-page, Lenis upgrade, script tags)</name>
  <files>
    index.html
    about.html
    contact.html
    careers.html
    capabilities/costume.html
    capabilities/dolls.html
    capabilities/plastic-toys.html
    capabilities/plush.html
    capabilities/rc-vehicle.html
    cn/index.html
    cn/about.html
    cn/contact.html
    cn/careers.html
    cn/capabilities/costume.html
    cn/capabilities/dolls.html
    cn/capabilities/plastic-toys.html
    cn/capabilities/plush.html
    cn/capabilities/rc-vehicle.html
    id/index.html
    id/about.html
    id/contact.html
    id/careers.html
    id/capabilities/costume.html
    id/capabilities/dolls.html
    id/capabilities/plastic-toys.html
    id/capabilities/plush.html
    id/capabilities/rc-vehicle.html
  </files>
  <action>
Make three changes to every non-admin HTML file across en/cn/id variants:

**Change A — Add data-page attribute to every `<body>` tag (FOUND-07):**
Use these exact values (consistent across all language variants — same page = same data-page):
- index.html → `<body data-page="home">`
- about.html → `<body data-page="about">`
- contact.html → `<body data-page="contact">`
- careers.html → `<body data-page="careers">`
- capabilities/costume.html → `<body data-page="capabilities-costume">`
- capabilities/dolls.html → `<body data-page="capabilities-dolls">`
- capabilities/plastic-toys.html → `<body data-page="capabilities-plastic-toys">`
- capabilities/plush.html → `<body data-page="capabilities-plush">`
- capabilities/rc-vehicle.html → `<body data-page="capabilities-rc-vehicle">`

Apply the same data-page values to cn/ and id/ variants of each page.

**Change B — Upgrade Lenis CDN from 1.1.18 to 1.3.18 (FOUND-02):**

For the 9 root files that already have `lenis@1.1.18`:
Replace: `<script src="https://unpkg.com/lenis@1.1.18/dist/lenis.min.js"></script>`
With:    `<script src="https://cdn.jsdelivr.net/npm/lenis@1.3.18/dist/lenis.min.js"></script>`

Note: Use jsdelivr CDN (consistent with GSAP delivery) instead of unpkg.

For cn/index.html and id/index.html: The Lenis comment exists but NO script tag. Add the Lenis 1.3.18 script tag immediately after the `<!-- Lenis Smooth Scroll -->` comment.

For all other cn/ and id/ files (about, contact, careers, all capabilities/): These have NO Lenis tag at all. Add Lenis 1.3.18 script tag in the scripts block, BEFORE the Splitting.js tag (CDN load order: Lenis → Splitting → GSAP → local scripts).

**Change C — Add new animation module script tags:**
After the existing `animations.js` script tag (or after the last existing local script tag), add the three new modules IN ORDER (dependency order matters):

For root-level pages (assets/ is adjacent):
```html
<script src="assets/animation/controller.js"></script>
<script src="assets/animation/scroll-engine.js"></script>
<script src="assets/animation/responsive.js"></script>
```

For cn/ pages (one level deep):
```html
<script src="../assets/animation/controller.js"></script>
<script src="../assets/animation/scroll-engine.js"></script>
<script src="../assets/animation/responsive.js"></script>
```

For cn/capabilities/ and id/capabilities/ pages (two levels deep):
```html
<script src="../../assets/animation/controller.js"></script>
<script src="../../assets/animation/scroll-engine.js"></script>
<script src="../../assets/animation/responsive.js"></script>
```

**Important notes:**
- Do NOT modify admin.html
- The existing `animations.js` script tag must remain — do NOT remove it
- cn/ and id/ pages currently load GSAP 3.12.7 — DO NOT upgrade GSAP version in this task (scope: Lenis only)
- cn/ and id/ pages that lack `animations.js` currently (they do not have it) — add the new scripts after `shared.js`
- Verify by checking that the script load order per page is: Lenis → Splitting → shared.js → GSAP → ScrollTrigger → animations.js (if present) → controller.js → scroll-engine.js → responsive.js
  </action>
  <verify>
    <automated>node -e "
const fs = require('fs');
const path = require('path');
const base = '/Users/charles/Projects/royal-regent/rr-website';

const checks = [
  // [file, data-page value, hasLenis, script depth]
  ['index.html', 'home', true, ''],
  ['about.html', 'about', true, ''],
  ['contact.html', 'contact', true, ''],
  ['careers.html', 'careers', true, ''],
  ['capabilities/plastic-toys.html', 'capabilities-plastic-toys', true, '../'],
  ['cn/index.html', 'home', true, '../'],
  ['cn/about.html', 'about', true, '../'],
  ['id/index.html', 'home', true, '../'],
  ['cn/capabilities/dolls.html', 'capabilities-dolls', true, '../../'],
  ['id/capabilities/rc-vehicle.html', 'capabilities-rc-vehicle', true, '../../'],
];

let pass = true;
checks.forEach(function(c) {
  const [file, page, hasLenis, depth] = c;
  const src = fs.readFileSync(path.join(base, file), 'utf8');

  if (!src.includes('data-page=\"' + page + '\"')) {
    console.error('FAIL data-page: ' + file + ' (expected ' + page + ')'); pass=false;
  }
  if (hasLenis && !src.includes('lenis@1.3.18')) {
    console.error('FAIL lenis@1.3.18: ' + file); pass=false;
  }
  if (src.includes('lenis@1.1.18')) {
    console.error('FAIL old lenis@1.1.18 still present: ' + file); pass=false;
  }
  const ctrlPath = depth + 'assets/animation/controller.js';
  if (!src.includes(ctrlPath)) {
    console.error('FAIL controller.js missing: ' + file + ' (expected ' + ctrlPath + ')'); pass=false;
  }
  console.log('OK: ' + file);
});

console.log(pass ? 'ALL HTML CHECKS PASSED' : 'HTML CHECKS FAILED');
process.exit(pass ? 0 : 1);
"
    </automated>
  </verify>
  <done>All 27 HTML files have data-page on body tag. All files with lenis@1.1.18 now use lenis@1.3.18. All files load controller.js, scroll-engine.js, responsive.js in correct relative path order after existing scripts. No file has lenis@1.1.18 remaining.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify foundation infrastructure in browser</name>
  <action>Human verification of animation infrastructure: confirm window.RR registry, Lenis smooth scroll, prefers-reduced-motion gate, CDN graceful degradation, and data-page attributes across pages.</action>
  <what-built>
    Three new animation infrastructure modules created and wired into all 27 HTML pages:
    - assets/animation/controller.js — window.RR registry with init/kill/refresh lifecycle
    - assets/animation/scroll-engine.js — Lenis 1.3.18 + GSAP ScrollTrigger single RAF loop
    - assets/animation/responsive.js — gsap.matchMedia() with prefers-reduced-motion gate
    All HTML pages have data-page on body. Lenis upgraded 1.1.18 → 1.3.18.
  </what-built>
  <how-to-verify>
    1. Start a local HTTP server: `python3 -m http.server 8000` in the project root
    2. Open http://localhost:8000 in browser DevTools → Console tab
    3. Expected console output (no errors):
       - [RR:scroll-engine] Lenis + ScrollTrigger sync initialized
       - [RR:responsive] gsap.matchMedia contexts registered
    4. Open DevTools Console and run: `window.RR` — should return object with modules, register, initAll, killAll, refreshAll
    5. Check smooth scroll works: scroll the page — motion should feel smooth/inertial (not instant)
    6. Test reduced motion: DevTools → Rendering → Emulate CSS media → prefers-reduced-motion: reduce → Reload → above-fold elements should appear immediately (no delay/hidden state)
    7. Test CDN degradation: In DevTools Network tab, block `unpkg.com` (or `lenis` requests) → Reload → Page should still render and navigate normally (no broken layout)
    8. Check http://localhost:8000/about.html — verify `window.RR` exists and console shows no errors
    9. Check http://localhost:8000/cn/index.html — verify `window.RR` exists (cn/ pages use ../assets/ path)
    10. In DevTools Console: `document.body.dataset.page` — should return "home" on index.html, "about" on about.html, etc.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe which checks failed with error messages from DevTools</resume-signal>
</task>

</tasks>

<verification>
Phase 1 foundation is complete when ALL of the following are true:
1. `window.RR` object accessible in browser console on any page
2. `window.RR.lenis` is a Lenis instance (not undefined) when CDN loads
3. `window.RR.mm` is a gsap.matchMedia context (not undefined) when GSAP loads
4. No console errors on any page load (warnings acceptable for missing CDN)
5. `document.body.dataset.page` returns non-empty string on every page
6. Smooth scroll behavior noticeable when scrolling any page
7. With prefers-reduced-motion enabled: no animation delay, all content immediately visible
8. Blocking the Lenis CDN: page loads and scrolls (natively) without JavaScript errors
</verification>

<success_criteria>
- window.RR registry operational with register/initAll/killAll/refreshAll
- Lenis + ScrollTrigger synchronized via single RAF loop (no scroll drift)
- All 27 HTML body tags carry data-page attribute
- Lenis upgraded to 1.3.18 on all pages that had 1.1.18
- prefers-reduced-motion immediately shows all content without animation
- CDN failure does not break page rendering or navigation
- document.fonts.ready wrapper available via window.RR.onFontsReady()
- All ScrollTrigger-capable contexts wrapped in gsap.matchMedia() in responsive.js
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` with:
- What was built (file list, key decisions)
- Actual patterns used (confirm or note deviations from plan)
- Any issues encountered and how they were resolved
- Confirmed window.RR API surface for Phase 2 to build on
</output>
